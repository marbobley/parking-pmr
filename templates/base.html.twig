<!DOCTYPE html>
<html lang="fr">
<head>
    {% set titleSite = "Carte Montpellier Parking PMR" %}
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description"
          content="Une carte google affichant la liste des places de parking pour personne à mobilité réduite sur Montpellier">
    <meta name="keywords" content="carte, pmr , personne à mobilité réduite, Montpellier, parking, places">
    <title>{% block title %}{{ titleSite }}{% endblock %}</title>
    <link rel="icon" href="{{ asset('images/favicon.svg') }}" sizes="any" type="image/svg+xml">
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">{{ titleSite }}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% set currentRoute = app.request.attributes.get('_route') %}
                <li class="nav-item">
                    <a class="nav-link {{ currentRoute == 'app_home' ? 'active' : '' }}" aria-current="page"
                       href="{{ path('app_home') }}">Accueil</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<main class="container my-4 flex-grow-1">
    {% block body %}{% endblock %}
</main>

<footer class="mt-auto bg-dark text-light py-3">
    <div class="container d-flex flex-column flex-md-row justify-content-between align-items-center gap-2">
        <div class="small">
            &copy; {{ "now"|date("Y") }} {{ titleSite }} - Tous droits réservés - Contact : fournier.wilf@gmail.com
        </div>
        {% block footer_extra %}{% endblock %}
    </div>
</footer>
</body>
</html>
